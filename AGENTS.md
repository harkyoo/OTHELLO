# AGENTS.md

## 목적
- 이 저장소는 브라우저에서 실행되는 오델로 게임 MVP를 만드는 것을 목표로 한다.
- 구현은 `HTML`, `CSS`, `JavaScript`를 기반으로 하며, UI는 React 컴포넌트 구조로 작성한다.
- 초기 범위는 로컬 2인 플레이어(PvP)이며, 이후 AI 대전 모드로 확장 가능해야 한다.

## 기준 문서
- 코드를 작성하기 전, 반드시 아래의 memory-bank 폴더의 파일들을 순서대로 읽어주세요.
1. memory-bank/game-design-document.md (게임 기획서)
2. memory-bank/implementation-plan.md (구현 계획서)
3. memory-bank/progress.md (현재 진행 상황)
- 구현 중 문서와 코드가 충돌하면, 임의 해석보다 기준 문서에 맞추는 것을 우선한다.

- 주요 기능 개발이나 마일스톤을 완료한 후에는, 아래 파일들을 업데이트해야 한다.
1. memory-bank/progress.md (진행 상황)

## 기술 스택
- 마크업: `HTML5`
- 스타일링: `CSS3`
- 프로그래밍 언어: `JavaScript (ES6+)`
- UI 구성: `React` 컴포넌트 기반
- 빌드 및 개발 환경:
  - `Vite` 기반 React 프로젝트를 기본 선택으로 권장한다.
  - 이유는 빠른 개발 서버, 단순한 설정, React 프로젝트 유지보수 편의성 때문이다.
- 패키지 관리:
  - `npm`을 기본값으로 사용한다.
- 테스트:
  - 게임 규칙 로직은 단위 테스트가 가능해야 한다.
  - 테스트 도구는 프로젝트 도입 시 `Vitest` 또는 이에 준하는 가벼운 테스트 환경을 우선 검토한다.

## 핵심 설계 원칙
- 게임 규칙 로직과 화면 렌더링 로직은 반드시 분리한다.
- React 컴포넌트는 UI 표현과 사용자 상호작용에 집중하고, 오델로 규칙 판단은 별도 로직 계층에서 처리한다.
- 상태 변경은 예측 가능해야 하며, 한 곳에서 추적 가능해야 한다.
- 작은 컴포넌트와 작은 함수로 나누어 유지보수성을 높인다.
- 확장 가능성을 고려하되, MVP 범위를 넘는 추상화는 피한다.

## 권장 개발 구조
- 아래 구조를 기본 방향으로 삼는다.

```text
/
  src/
    components/
      Board.jsx
      Cell.jsx
      ScoreBoard.jsx
      StatusBar.jsx
      RestartButton.jsx
    game/
      constants.js
      board.js
      rules.js
      gameState.js
    hooks/
      useOthelloGame.js
    styles/
      reset.css
      globals.css
      board.css
      ui.css
    App.jsx
    main.jsx
  public/
  index.html
```

## 디렉터리 규칙
- `components/`: 화면에 보이는 React 컴포넌트만 둔다.
- `game/`: 오델로 규칙, 보드 계산, 턴 전환, 승패 판정 등 순수 로직을 둔다.
- `hooks/`: UI와 게임 로직을 연결하는 커스텀 훅을 둔다.
- `styles/`: 전역 스타일과 컴포넌트/기능별 스타일을 분리한다.
- 루트의 `index.html`은 최소 진입점 역할만 하도록 유지한다.

## React 컴포넌트 규칙
- 컴포넌트는 하나의 책임만 갖도록 만든다.
- `Board`는 보드 전체 렌더링과 셀 배치를 담당한다.
- `Cell`은 개별 칸의 표시와 클릭 이벤트만 담당한다.
- `ScoreBoard`는 흑/백 점수 표시만 담당한다.
- `StatusBar`는 현재 턴, 패스, 종료 메시지 표시를 담당한다.
- 게임 상태 계산을 컴포넌트 내부에 과도하게 넣지 않는다.
- 중복 렌더링을 줄이되, 성급한 최적화보다 구조 명확성을 우선한다.

## 상태 관리 규칙
- 게임의 단일 진실 공급원(single source of truth)을 유지한다.
- 최소 상태만 저장하고, 계산 가능한 값은 가능한 한 파생 값으로 처리한다.
- 예를 들어 아래 값은 중앙 상태로 관리할 수 있다.
  - 보드 상태
  - 현재 턴
  - 게임 종료 여부
  - 최근 액션 또는 상태 메시지
- 아래 값은 필요 시 계산으로 구할 수 있다.
  - 유효한 착수 위치
  - 흑/백 점수
  - 승자 정보

## 게임 로직 규칙
- 보드는 8x8 고정이다.
- 좌표 체계는 한 번 정하면 프로젝트 전체에서 일관되게 유지한다.
- 초기 배치는 기준 문서에 맞게 설정한다.
  - 백: `(3,3)`, `(4,4)`
  - 흑: `(3,4)`, `(4,3)`
- 흑이 선공이다.
- 유효한 착수 판정, 뒤집기 계산, 패스 처리, 종료 판정은 UI 코드와 분리된 함수에서 처리한다.
- 게임 규칙 함수는 가능하면 순수 함수로 작성한다.

## 유지보수 최적화 규칙
- 함수 하나에 여러 책임을 넣지 않는다.
- 컴포넌트 하나가 규칙 계산과 화면 제어를 동시에 과도하게 담당하지 않게 한다.
- 하드코딩된 숫자와 문자열은 상수로 분리한다.
- 조건문이 복잡해지면 보조 함수로 분리한다.
- 재사용 가능한 규칙 로직은 `game/` 하위로 모은다.
- UI 문구는 한 곳에서 관리할 수 있게 정리하는 것을 권장한다.
- 파일명, 함수명, 상태명은 역할이 드러나게 작성한다.
- 디버깅을 어렵게 만드는 과도한 축약 이름은 피한다.

## CSS 작성 규칙
- 스타일은 레이아웃, 보드, UI 영역이 섞이지 않게 분리한다.
- 전역 스타일은 최소화하고, 컴포넌트나 기능 단위로 책임을 나눈다.
- 색상, 간격, 보드 크기 등 반복되는 값은 CSS 변수로 관리하는 것을 권장한다.
- 데스크톱 우선으로 설계하되, 모바일에서도 플레이 가능한 반응형 레이아웃을 고려한다.
- 애니메이션은 MVP에서 필수가 아니며, 추가 시 게임 상태 전달을 방해하지 않도록 한다.

## JavaScript 작성 규칙
- 최신 표준 문법을 사용하되, 읽기 쉬운 코드를 우선한다.
- 복잡한 배열 변환이나 중첩 조건은 단계별 변수로 풀어 쓴다.
- 사이드 이펙트는 경계를 분명히 둔다.
- DOM 직접 조작은 지양하고 React 상태 기반 렌더링을 사용한다.
- 규칙 검증 로직은 테스트 가능한 독립 함수로 유지한다.

## 권장 구현 순서
1. React 앱 기본 구조 생성
2. 8x8 보드 데이터 모델 구현
3. 초기 보드 상태 생성 로직 구현
4. 유효한 착수 판별 함수 구현
5. 착수 시 뒤집기 처리 구현
6. 턴 전환 및 패스 처리 구현
7. 종료 판정 및 승패 계산 구현
8. React 컴포넌트와 게임 로직 연결
9. 점수, 상태 메시지, 재시작 UI 추가
10. 주요 규칙 테스트 작성
11. 반응형 UI와 마무리 스타일 정리

## 테스트 우선 포인트
- 초기 돌 배치가 정확한지
- 유효하지 않은 위치에는 착수할 수 없는지
- 한 방향 및 여러 방향 뒤집기가 모두 정확한지
- 둘 수 없는 경우 패스가 정상 동작하는지
- 양측 모두 둘 수 없을 때 종료되는지
- 점수와 승패 계산이 올바른지
- 재시작 시 전체 상태가 초기화되는지

## 금지 사항
- 게임 규칙 로직을 여러 컴포넌트에 분산 구현하지 않는다.
- UI 처리 편의를 위해 규칙 정확성을 희생하지 않는다.
- MVP 단계에서 AI, 온라인 대전, 저장 기능을 기본 구현 범위로 포함하지 않는다.
- 하나의 거대한 컴포넌트에 모든 상태와 UI를 몰아넣지 않는다.

## 완료 기준
- 사용자가 브라우저에서 오델로 게임을 처음부터 끝까지 정상적으로 플레이할 수 있다.
- React 컴포넌트 구조로 UI가 구성되어 있다.
- 게임 규칙 로직이 UI와 분리되어 있다.
- 착수 검증, 뒤집기, 패스, 종료, 승패 판정이 정확히 동작한다.
- 점수 표시, 현재 턴 표시, 상태 메시지, 재시작 기능이 제공된다.
- 이후 AI 모드 추가가 가능한 구조로 유지된다.
